radius = float(input("Enter the radius"))
height = float(input("Enter the height"))
p = 3.142
surface_area = (p * radius * radius) + (p * radius * 2 * height)
volume = p * radius * radius * height
print(surface_area)
print(volume)
